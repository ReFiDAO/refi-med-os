# 2026-04-28 — Bootstrap session

**Operator:** Luiz Fernando (scaffolding maintainer)
**Session type:** instance scaffolding (pre-bootstrap)

## Summary

Scaffolded `refi-med-os` from `organizational-os-framework@3.0` as the new
operational instance for ReFi Mediterranean. Federated as a peer of
`refi-dao-os`, `refi-bcn-os`, and `regen-coordination-os` under the `refi-dao`
network. Hub at `github.com/ReFiDAO`.

Consolidated the public website + knowledge-base repo
(`ReFiDAO/ReFi-Mediterranean`, Quartz) into `repos/refi-mediterranean/` —
preserves its independent git remote and history; this instance rehydrates it
via `npm run clone:repos`.

## Decisions

- **Hosting:** `refi-med-os` lives at `github.com/ReFiDAO/refi-med-os` (public),
  matching the pattern of `ReFiDAO/ReFi-Mediterranean`. Decoupled from
  `regen-coordination` org.
- **Consolidation shape:** existing `ReFi-Mediterranean` repo moved into
  `repos/refi-mediterranean/` (preserves `.git`), not as a submodule and not
  as a symlink. `repos/*` stays gitignored; `repos.manifest.json` is the
  rehydration source.
- **Pre-fill scope:** minimal stubs only. Identity files are intentionally
  "TBD via bootstrap interview" so the operator following tomorrow's one-pager
  walks through `bootstrap-interviewer` cleanly without inheriting our
  guesses.

## What's left for the operator

See `BOOTSTRAP.md` — TL;DR is `/initialize` → bootstrap interview →
`npm run generate:schemas` → `/close`.
